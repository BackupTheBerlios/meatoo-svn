
You'll need to do some ugly apache config voodoo:


<Location /meatoo>
    ProxyPass http://localhost:8080
    ProxyPassReverse http://localhost:8080
    RequestHeader set CP-Location /meatoo
    SetEnv force-proxy-request-1.0 1
</Location>

# We need one of these sections for each password
# protected area. Yeck.

<Location /add_known>
    ProxyPass http://localhost:8080/add_known
    ProxyPassReverse http://localhost:8080/add_known
    RequestHeader set CP-Location /add_known
    SetEnv force-proxy-request-1.0 1
</Location>

<Location /ignore>
    ProxyPass http://localhost:8080/ignore
    ProxyPassReverse http://localhost:8080/ignore
    RequestHeader set CP-Location /ignore
    SetEnv force-proxy-request-1.0 1
</Location>

<Location /login>
    ProxyPass http://localhost:8080/login
    ProxyPassReverse http://localhost:8080/login
    RequestHeader set CP-Location /login
    SetEnv force-proxy-request-1.0 1
</Location>

